# Copie para .env.local (desenvolvimento) ou configure no Railway (produção).

# Caminho do banco SQLite. No Railway aponte para o volume persistente:
DATABASE_PATH=/data/presenca.db

# Senha do administrador (área /admin). Troque por algo forte!
ADMIN_PASSWORD=troque-esta-senha